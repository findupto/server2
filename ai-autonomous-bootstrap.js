const express=require('express');const aiAutonomous=require('./ai-autonomous-routes');const original=express.application.listen;express.application.listen=function(...args){const router=this._router;const before=router?.stack?.length||0;aiAutonomous(this);if(router?.stack&&router.stack.length>before){const added=router.stack.splice(before);router.stack.unshift(...added)}return original.apply(this,args)};