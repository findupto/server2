const express=require('express');const layer=require('./financial-hardening-routes');const original=express.application.listen;if(!express.application.__mkFinancialHardening){express.application.listen=function(...args){const router=this._router;const before=router?.stack?.length||0;if(!this.__mkFinancialHardening){try{layer(this);this.__mkFinancialHardening=true}catch(e){console.error('Financial hardening load failed:',e.message)}}if(router?.stack){const added=router.stack.splice(before);const firstRoute=router.stack.findIndex(layer=>layer&&layer.route);router.stack.splice(firstRoute<0?router.stack.length:firstRoute,0,...added)}return original.apply(this,args)}}
