const express=require('express');const routes=require('./ai-realtime-routes');const original=express.application.listen;express.application.listen=function(...args){const router=this._router;const before=router?.stack?.length||0;routes(this);if(router?.stack&&router.stack.length>before){const added=router.stack.splice(before);router.stack.unshift(...added)}return original.apply(this,args)};