const express=require('express');const routes=require('./ai-realtime-routes');const original=express.application.listen;express.application.listen=function(...args){this.use(routes);return original.apply(this,args)};